import { css } from "styled-components";

const cssModalBackground = css`
  z-index: 500;

  position: fixed;
  inset: 0;

  display: flex;

  background-color: rgba(0, 0, 0, 0.3);
`;

export { cssModalBackground };
