const ROUTE = {
  LANDING: "/",
  LOGIN: "/login",
  HOME: "/home",
  PROFILE: "/profile",
  POST: "/post",
  CHAT: "/chat",
};

const ROUTE_LOGIN = {
  JOIN: "join",
  AUTHORIZE: "authorize",
};

const ROUTE_CHAT = {
  CHATROOM: "chatroom",
};

export { ROUTE_LOGIN, ROUTE_CHAT };

export default ROUTE;
